{
  "name": "Nombre del Proyecto",
  "dockerFile": "Dockerfile",
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "extensions": [
    "ms-python.python"
  ],
  "postCreateCommand": "./install_dependencies.sh" // Comando para ejecutar el script de instalación después de crear el entorno
}
